# docker-compose の PostgreSQL 設定（コピーして .env を作成）
#   cp .env.example .env
POSTGRES_USER=laws
POSTGRES_PASSWORD=laws
POSTGRES_DB=laws
POSTGRES_PORT=5432

# アプリ／取り込みからの接続 URL（SQLAlchemy async・Procrastinate 用、後続フェーズで使用）
DATABASE_URL=postgresql+asyncpg://laws:laws@localhost:5432/laws
